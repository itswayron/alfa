package dev.weg.alfa.modules.models.movement

enum class MovementStatus {
    COMPLETED,
    CANCELLED,
    SCHEDULED,
    REQUESTED,
    QUOTATION,
    PENDING,
    AWAITING_DELIVERY,
    AWAITING_INVOICE_ENTRY,
}
