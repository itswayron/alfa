CREATE TABLE statuslending (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    status_lending VARCHAR(100) NOT NULL UNIQUE
);

INSERT INTO statuslending (status_lending) VALUES
('ATIVO'),
('DEVOLVIDO'),
('ATRASADO'),
('RESERVADO');